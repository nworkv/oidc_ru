# Порт сервера
PORT=8085

# Публичный URL этого сервера (используется как redirect_uri)
APP_URL=http://localhost:8085

# URL сервера авторизации (Keycloak и др.) — для справки
AUTH_SERVER_URL=http://localhost:8080
